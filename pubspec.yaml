name: smart_one
description: A new Flutter application.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^0.5.1+2
#  cached_network_image: ^0.8.0
  http: ^0.12.0+2
  transparent_image: ^1.0.0
  flutter_socket_io: ^0.4.6
  adhara_socket_io: ^0.1.10
  fluttertoast: ^3.0.4
  fzxing: ^0.3.0
  mqtt_client: ^5.5.3
  image: ^2.0.9
#  path_provider: ^1.1.0
  extended_image: ^0.4.1
  sqflite: ^1.1.6+1
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - images/app_logo.png
    - images/btn_draw_path.png
    - images/curriculum_selected.png
    - images/curriculum_unselected.png
    - images/icon_calendar.png
    - images/icon_center_screen.png
    - images/icon_first_page.png
    - images/icon_fit_screen.png
    - images/icon_last_page.png
    - images/icon_left_right.png
    - images/icon_rote_left.png
    - images/icon_rote_right.png
    - images/icon_scale_big.png
    - images/icon_scale_small.png
    - images/icon_start_course.png
    - images/icon_up_down.png
    - images/kcb_selected.png
    - images/kcb_unselected.png
    - images/mine_selected.png
    - images/mine_unselected.png
    - images/note_selected.png
    - images/note_unselected.png
    - images/pc_connected.png
    - images/pc_disconnected.png
    - images/pc_logo.png
    - images/pdf.png
    - images/ppt.png
    - images/scan_camera.png
    - images/none_image.png
    - images/bold_paint_selected.png
    - images/bold_paint_unselected.png
    - images/clear_path.png
    - images/clear_path_selected.png
    - images/icon_dianming.png
    - images/icon_course_qiandao.png
    - images/icon_qiangda.png
    - images/tab_note_book_selected.png
    - images/tab_note_book_unselected.png
    - images/tab_table_selected.png
    - images/tab_table_unselected.png
  # To add assets to your application, add an assets section, like this:
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
